@model Church.WebApp.Models.ApplicationUser
@{
    ViewData["Title"] = "Logowanie";
}

<div class="row shadow-lg" style="max-width: 1040px; margin: auto; min-height: 80vh;">
    <div class="container">
        <div class="col-md-6 p-4">
            <form asp-action="Login">
                <h2>Logowanie</h2>
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="UserName" class="control-label">Nazwa użytkownika</label>
                    <input asp-for="UserName" class="form-control" />
                    <span asp-validation-for="UserName" class="text-danger"></span>
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="Password" class="control-label">Hasło</label>
                    <input type="password" asp-for="Password" class="form-control" autofocus />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <br />
                <div class="form-group">
                    <input type="submit" value="Zaloguj" class="btn btn-default btn-primary" />
                </div>

                <input type="hidden" asp-for="ReturnUrl" />
            </form>
            <p>Po zalogowaniu, udostepnione zostaną przekłady oznaczone flagą <span class="badge bg-danger">wymaga hasła</span>. Dostęp do tych treści przeznaczony jest wyłącznie dla członków zboru.</p>
        </div>
    </div>
</div>

